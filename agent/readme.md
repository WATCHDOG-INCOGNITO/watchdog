# Watchdog Agent

`agent/`는 스캔 실행/수집/분석을 담당하는 에이전트 영역이다. (WIP)

현재 리포는 백엔드 API 계약을 먼저 고정하기 위해 `backend/`에 Django/DRF 기반 Stub API를 포함한다.

## Backend 연동(초안)

백엔드 엔드포인트 및 응답 형태는 `backend/API_CONTRACT.md`와 `backend/readme.md`를 기준으로 한다.
